matchid: int

teamone: string
teamtwo: string
oddsone: float
oddstwo: float
oddstie: float
sport: string
country: string
league: string
datetime: DATETIME
completed: boolean
// need way to find if cancelled


select oddsone, oddstwo from table where sport == soccer and league == bundelega



MatchId int NOT NULL PRIMARY KEY, 
TeamOne varchar(255),
TeamTwo varchar(255), 
Description varchar(255),
OddsOne float(6), 
OddsTwo float(6), 
OddsTie float(6), 
Sport varchar(255),
Country varchar(255),
League varchar(255),
DateTime datetime, 
Completed boolean NOT NULL DEFAULT false

leagues
14

filters in json form: 
{
    key: value, 
    key: value
}


filter_headers = ['MatchId', 'Team', 'OddsWin', 'OddsTie', 'Sport', 'Country', 'League', 'DateTime', 'Completed']
